apiVersion: v1
kind: Service
metadata:
  name: {{ include "pluma.fullname" . }}
  labels:
    {{- include "pluma.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pluma.selectorLabels" . | nindent 4 }}
